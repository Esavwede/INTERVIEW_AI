{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "sourceMap": true,         // Enable source maps
    "inlineSources": true,     // Include original source in the map
    "removeComments": true
  },
  "exclude": [
    "spec",
    "src/public/",
    "build.ts",
    "dist"
  ]
}
